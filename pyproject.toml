[project]
name = "job-hunter"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "playwright",
    "apscheduler",
    "aiohttp",
    "feedparser",
    "python-dotenv",
]
